# PyInstaller configuration for IP Finder
# This script builds the executable for the installer
# Run: pyinstaller --onefile --console --name IPFinder --add-data "keywords.txt:." local_dir_parser.py

# No spec file needed - build.bat/build.py use command-line arguments instead

